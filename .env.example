# 부평 플리마켓 환경변수 (.env.local 로 복사해서 채우세요)
# 모두 비워두면 앱은 로컬 어댑터(localStorage)로 동작합니다.

# --- Supabase (기기 간 실시간 + 영구 저장) ---
# Supabase 대시보드 → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# 서버 전용(절대 클라이언트 노출 금지). 마이그레이션/관리 작업용.
SUPABASE_SERVICE_ROLE_KEY=

# --- 관리자 비밀번호 (PoC) ---
NEXT_PUBLIC_ADMIN_PASSWORD=bupyeong

# --- (후속) 기능 6-B 지도 ---
# NEXT_PUBLIC_KAKAO_MAP_KEY=
